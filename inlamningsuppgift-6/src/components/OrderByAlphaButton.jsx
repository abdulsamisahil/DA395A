const OrderByAlphaButton = ({ clickHandler }) => {
  return (
    <button onClick={clickHandler} className='btn btn-primary'>
      Alfabetisk ordning
    </button>
  )
}

export default OrderByAlphaButton
